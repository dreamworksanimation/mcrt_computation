Import('env')
 
env.Tool('dwa_doxygen')
 
#the name of the folio
folioName = "LIBNAME"
 
# Files and directories to use as input to Doxygen
inputFiles = ['#computation']
 
dox = env.DWADoxygenSimple(folioName, inputFiles)
